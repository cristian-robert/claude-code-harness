---
type: index
folder: system/schemas
updated: 2026-07-06
tags:
  - index
---

# system / schemas

The machine-readable contracts notes in this vault follow. Change the schema here **first**, then update affected notes.

## Contents

- [[system/schemas/frontmatter|frontmatter.md]] — the frontmatter (properties) contract for every note type.

## Agent SOP

1. Before adding a new note type or property, define it in `frontmatter.md`.
2. Keep the taxonomy small — add a type only when an existing one genuinely doesn't fit.
